<Window x:Class="WPF_Project.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Project"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        Closing="Window_Closing">
    <Grid Background="LightSteelBlue">
        <!-- Defining Columns -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="50"/>
        </Grid.ColumnDefinitions>
        <!-- Defining Rows -->
        <Grid.RowDefinitions>
            <RowDefinition Height="75"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="70"/>
            <RowDefinition Height="70"/>
        </Grid.RowDefinitions>

        <!-- Welcoming text -->
        <StackPanel HorizontalAlignment="Center" Grid.ColumnSpan="3" Grid.Column="1" Orientation="Horizontal">
            <TextBlock Text="Welcome to Audi!" FontSize="35" FontWeight="Bold" Margin="0,15,0,0"/>
        </StackPanel>

        <!-- Description -->
        <StackPanel Grid.Column="1" Grid.ColumnSpan="3" Width="300" Grid.Row="1" Margin="0,0,0,30">
            <TextBlock Text="As an employee of dealership, Audi, you have the ability to manipulate the system!"
                   FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" TextAlignment="Center"/>
        </StackPanel>

        <!-- Buttons -->
        <Button Cursor="Hand" Content="Add a car" FontSize="16" FontWeight="DemiBold" Grid.Row="2" Grid.Column="1" Width="100" Height="50" Click="btnAdd_Click"/>
        <Button Cursor="Hand" Content="Update a car" FontSize="16" FontWeight="DemiBold" Grid.Row="2" Grid.Column="2" Width="105" Height="50" Click="btnUpdate_Click"/>
        <Button Cursor="Hand" Content="Delete a car" FontSize="16" FontWeight="DemiBold" Grid.Row="2" Grid.Column="3" Width="100" Height="50" Click="btnDelete_Click"/>
        <Button Cursor="Hand" Content="Show all cars" FontSize="16" FontWeight="DemiBold" Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Width="115" Height="50" Grid.RowSpan="2" Margin="0,0,25,0" Click="btnShowAll_Click"/>
        <Button Cursor="Hand" Content="Shopping List" FontSize="16" FontWeight="DemiBold" Grid.Row="3" Grid.Column="2" Grid.ColumnSpan="2" Width="115" Height="50" Grid.RowSpan="2" Click="btnShoppingList_Click"/>

    </Grid>
</Window>
